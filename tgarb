-- LocalScript داخل StarterGui

-- إنشاء GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

local Frame = Instance.new("Frame")
Frame.Size = UDim2.new(0, 180, 0, 100)
Frame.Position = UDim2.new(0.05, 0, 0.8, 0)
Frame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Frame.BackgroundTransparency = 0.4
Frame.Parent = ScreenGui
Frame.Active = true
Frame.Draggable = true

local FlyButton = Instance.new("TextButton")
FlyButton.Size = UDim2.new(0, 160, 0, 35)
FlyButton.Position = UDim2.new(0, 10, 0, 10)
FlyButton.Text = "طيران"
FlyButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
FlyButton.TextColor3 = Color3.fromRGB(255, 255, 255)
FlyButton.Parent = Frame

local SpeedBox = Instance.new("TextBox")
SpeedBox.Size = UDim2.new(0, 160, 0, 30)
SpeedBox.Position = UDim2.new(0, 10, 0, 55)
SpeedBox.PlaceholderText = "كم يرتفع في الثانية (مثلاً 9)"
SpeedBox.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
SpeedBox.TextColor3 = Color3.fromRGB(255, 255, 255)
SpeedBox.Text = ""
SpeedBox.Parent = Frame

-- من هنا يبدأ الفعل
local flying = false
local platform = nil

FlyButton.MouseButton1Click:Connect(function()
	local player = game.Players.LocalPlayer
	local char = player.Character or player.CharacterAdded:Wait()
	local hrp = char:WaitForChild("HumanoidRootPart")
	
	if flying == false then
		flying = true
		FlyButton.Text = "إيقاف الطيران"
		
		-- إنشاء البارت
		platform = Instance.new("Part")
		platform.Size = Vector3.new(6, 1, 6)
		platform.Anchored = true
		platform.Transparency = 0.5
		platform.Color = Color3.fromRGB(60, 60, 60)
		platform.Parent = workspace
		
		local speed = tonumber(SpeedBox.Text) or 9
		
		-- تحديث موقع البارت باستمرار
		task.spawn(function()
			while flying and platform do
				task.wait(0.1)
				-- يخليه يمشي مع اللاعب
				local hrpPos = hrp.Position
				platform.Position = Vector3.new(hrpPos.X, hrpPos.Y - 3, hrpPos.Z)
				-- يرفعه للأعلى تدريجياً
				hrp.CFrame = hrp.CFrame + Vector3.new(0, speed/10, 0)
			end
		end)
	else
		flying = false
		FlyButton.Text = "طيران"
		if platform then
			platform:Destroy()
			platform = nil
		end
	end
end)
