local Players = game:GetService("Players")
local Player = Players.LocalPlayer
local StarterGui = game:GetService("StarterGui")

-- دالة لإظهار إشعار روبلوكس الرسمي
local function showSystemNotification(title, text, duration)
    StarterGui:SetCore("SendNotification", {
        Title = title;
        Text = text;
        Duration = duration; -- المدة بالثواني
    })
end

-- دالة لحذف بارتات PetMover وإظهار الإشعار
local function removePetMovers()
    local count = 0
    for _, part in pairs(workspace:GetDescendants()) do
        if part:IsA("BasePart") and part.Name == "PetMover" then
            part:Destroy()
            count = count + 1
        end
    end
    if count > 0 then
        showSystemNotification("تم الحذف", "تم حذف "..count.." بارت/بارتات PetMover", 3)
    end
end

-- تشغيل أول مرة
removePetMovers()

-- تحديث مستمر لأي بارت جديد ينضاف
while true do
    task.wait(1)
    removePetMovers()
end
